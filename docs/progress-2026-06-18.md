# 2026-06-18 Progress

## 今天完成

- 关闭问号特殊升调。
  - `backend/app/services/tts.py`
  - Edge TTS `model_version=12`
  - 疑问句现在和普通句一样使用 `+0% / +0Hz`
  - 感叹句和省略号仍保留轻量差异

- 完善音频预生成体验。
  - 已有批量预热接口：`POST /api/audio/sentences/prefetch`
  - 已有状态查询接口：`POST /api/audio/sentences/status`
  - 前端显示句子音频状态点
  - 章节标题区有“预生成本章”按钮和进度

- 修复非 UTF-8 TXT 解析失败。
  - `backend/app/workers/parse_books.py`
  - TXT 读取按 `utf-8-sig`、`utf-8`、`gb18030`、`big5` 回退
  - 用户上传的《增广贤文》原因为 GBK/GB18030 编码，不是 UTF-8

- 修复中文断句正则。
  - `backend/app/services/text_splitter.py`
  - 使用稳定 Unicode 转义匹配中文句号、问号、叹号和常见右引号/括号

- 已重新解析用户上传失败的《增广贤文》。
  - book id: `f19c5b5a-2f0f-467e-a09a-4bfb5fae11b1`
  - status: `ready`
  - chapters: `1`
  - sentences: `350`

- 播放栏移到阅读正文上方。
  - `frontend/src/App.tsx`
  - `frontend/src/styles.css`
  - 底部旧栏目前用 CSS 隐藏

- 新增测试样例和上传辅助脚本。
  - `samples/tts-test-sample.txt`
  - `scripts/upload_sample.py`

## 今天验证

- `npm run build` 通过。
- 后端导入检查通过。
- 《增广贤文》数据库状态确认 `ready`。
- 本地解析函数确认可读取 GB18030 TXT。

## 当前注意事项

- `docs/PROJECT_MEMORY.md` 已确认按 UTF-8 读取时内容正常；如果 PowerShell 输出乱码，需要显式设置 UTF-8 输出编码。
- 后端代码变更后，需要用户手动重启服务才能在浏览器中生效。
- 不要默认由 Codex 启动/重启长期服务；Windows 后台启动在当前工具环境里不稳定。
- 建议用户使用：

```bat
scripts\stop-dev.bat
scripts\start-dev.bat
```

## 明天优先任务

做“已有书籍删除”。

建议范围：

1. 后端新增 `DELETE /api/books/{book_id}`。
2. 删除书籍时清理：
   - `books`
   - `book_files`
   - `chapters`
   - `paragraphs`
   - `sentences`
   - `audio_assets`
   - 相关 `jobs`
   - 上传源文件
   - 已生成音频文件
3. 前端书库列表给每本书增加删除入口。
4. 删除前弹确认。
5. 删除成功后刷新书库。
6. 如果删除的是当前选中书籍，清空：
   - `selectedBookId`
   - `chapters`
   - `currentSentenceId`
   - 播放状态
   - 音频缓存/预热状态

先做本地单用户版本，不做权限系统。

## 今天后续补充

- 已完成“已有书籍删除”：
  - 后端新增 `DELETE /api/books/{book_id}`
  - 删除数据库关联记录、上传源文件、已生成音频和相关任务
  - 前端书库列表新增删除入口
  - 删除当前选中书籍时清空阅读、播放、音频缓存和预热状态

- 已优化删除交互：
  - 将浏览器原生 `confirm` 改为页面内确认弹窗
  - 弹窗展示书名、删除影响范围、取消/确认按钮和删除中 loading

- 已更新 `docs/PROJECT_MEMORY.md`：
  - 补充 2026-06-18 交接记录
  - 补充编码注意事项
  - 修正当前项目能力和下一步优先任务

- 已完成阅读进度保存最小版：
  - 后端新增 `GET /api/books/{book_id}/progress`
  - 后端新增 `PUT /api/books/{book_id}/progress`
  - 使用默认本地用户 `local`
  - 保存时校验 `sentence_id` 必须属于当前书籍
  - 前端加载章节后尝试恢复上次句子
  - 点击句子、上一句/下一句切换当前句时保存进度

- 已验证：
  - `npm run build` 通过
  - 后端导入检查通过
  - ruff 检查通过
  - Python AST 语法检查通过
