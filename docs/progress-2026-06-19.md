# 2026-06-19 Progress

## 今天完成

- 优化已有书籍删除交互。
  - `frontend/src/App.tsx`
  - `frontend/src/styles.css`
  - 将浏览器原生 `window.confirm` 改为页面内确认弹窗
  - 弹窗展示书名、删除影响范围、取消/确认按钮和删除中 loading

- 处理项目文档乱码问题。
  - `docs/PROJECT_MEMORY.md`
  - `docs/progress-2026-06-18.md`
  - 确认 `PROJECT_MEMORY.md` 文件本身按 UTF-8 读取正常
  - 之前乱码是 PowerShell 输出编码问题
  - 已补充 UTF-8 读取说明
  - 已修正 `progress-2026-06-18.md` 中关于乱码的旧备注

- 完成阅读进度保存最小版。
  - `backend/app/schemas/book.py`
  - `backend/app/services/progress.py`
  - `backend/app/api/routes/books.py`
  - `frontend/src/types.ts`
  - `frontend/src/api.ts`
  - `frontend/src/App.tsx`
  - 后端新增 `GET /api/books/{book_id}/progress`
  - 后端新增 `PUT /api/books/{book_id}/progress`
  - 使用默认本地用户 `local`
  - 保存时校验 `sentence_id` 必须属于当前书籍
  - 前端加载章节后尝试恢复上次句子
  - 点击句子、上一句/下一句切换当前句时保存进度

- 更新项目记忆。
  - `docs/PROJECT_MEMORY.md`
  - 补充 2026-06-18/2026-06-19 相关完成项
  - 更新当前已完成能力
  - 更新下一步优先任务

- 完成精确播放位置保存/恢复。
  - `frontend/src/App.tsx`
  - 前端播放中每 5 秒保存一次 `audio_position_ms`
  - 暂停时立即保存当前播放位置
  - 切换上一句/下一句时保存新句子的播放位置为 `0`
  - 恢复书籍进度时保留后端返回的 `audio_position_ms`
  - 再次播放恢复出来的句子时设置 `<audio>.currentTime`

## 今天验证

- `npm run build` 通过。
- 后端导入检查通过。
- `ruff check backend/app` 通过。
- Python AST 语法检查通过。
- 用户已验证已有书籍删除功能可用。
- `npm run build` 在精确播放位置保存/恢复改动后再次通过。
- 用户已实机验证精确播放位置保存/恢复可用。

## 当前注意事项

- 后端代码变更后，需要用户手动重启服务才能在浏览器中生效。
- 不要默认由 Codex 启动/重启长期服务；Windows 后台启动在当前工具环境里不稳定。
- 建议用户使用：

```bat
scripts\stop-dev.bat
scripts\start-dev.bat
```

- Markdown 文档统一按 UTF-8 读取和写入。
- 如果 PowerShell 输出中文乱码，优先使用：

```powershell
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
Get-Content -Path docs\PROJECT_MEMORY.md -Encoding UTF8
```

## 下一个优先任务

准备落 Git 里程碑 `v0.1-local-listen-mvp`，然后进入测试与稳定性阶段。

建议范围：

1. 初始化 Git 并提交当前本地单用户 MVP 基线。
2. 补后端删除和进度接口的自动化测试。
3. 增加正式 E2E 测试脚本。
4. EPUB 解析。
5. 登录/多用户系统。

## 后续任务池

1. 补后端删除和进度接口的自动化测试。
2. 增加正式 E2E 测试脚本。
3. EPUB 解析。
4. 更自然的小说朗读体验。
5. 登录/多用户系统。
